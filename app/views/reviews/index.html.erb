<h1>Отзывы о животных</h1>
<p><%= link_to 'написать отзыв', new_review_path %></p>
<% @reviews.each do |review| %>
    <p><%= link_to (image_tag review.review_attachments.where(main:true).first.file.thumb), review_path(review.breed.translate, review) %>, <%= review.breed.name %></p>
<% end %>